import { TriangleRatingDirective } from './triangle-rating.directive';

describe('TriangleRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new TriangleRatingDirective();
    expect(directive).toBeTruthy();
  });
});
